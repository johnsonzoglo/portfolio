const cart=JSON.parse(localStorage.getItem('jz-market-cart')||'[]'),form=document.querySelector('#checkoutForm'),message=document.querySelector('#checkoutMessage');let products=[];const money=v=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(v),esc=v=>{const e=document.createElement('span');e.textContent=v;return e.innerHTML};function render(){const rows=cart.map(i=>({...i,product:products.find(p=>p.id===i.id)})).filter(r=>r.product);document.querySelector('#summaryItems').innerHTML=rows.map(r=>`<div class="summary-item"><div><h3>${esc(r.product.name)}</h3><p>Quantity ${r.quantity}</p></div><strong>${money(r.product.price*r.quantity)}</strong></div>`).join('');document.querySelector('#summaryCount').textContent=`${rows.reduce((s,r)=>s+r.quantity,0)} items`;document.querySelector('#summaryTotal').textContent=money(rows.reduce((s,r)=>s+r.product.price*r.quantity,0));if(!rows.length){message.textContent='Your cart is empty.';form.querySelector('button').disabled=true}}form.addEventListener('submit',async e=>{e.preventDefault();message.textContent='';const button=form.querySelector('button');button.disabled=true;const data=new FormData(form);try{const response=await fetch('/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({customer:{name:data.get('name'),email:data.get('email'),phone:data.get('phone'),address:data.get('address'),note:data.get('note')},paymentMethod:data.get('paymentMethod'),deliveryZone:data.get('deliveryZone'),discountCode:data.get('discountCode'),items:cart.map(i=>({id:i.id,quantity:i.quantity}))})});const result=await response.json();if(!response.ok)throw new Error(result.error);localStorage.removeItem('jz-market-cart');form.style.display='none';document.querySelector('#orderNumber').textContent=result.orderNumber;document.querySelector('#orderSuccess').classList.add('visible')}catch(error){message.textContent=error.message;button.disabled=false}});Promise.all([fetch('/api/products').then(r=>r.json()),fetch('/api/store-settings').then(r=>r.json())]).then(([productData,settings])=>{products=productData;document.querySelector('#deliveryZone').innerHTML=settings.deliveryZones.map(z=>`<option value="${esc(z.id)}">${esc(z.name)} — ${money(z.fee)}</option>`).join('');render()}).catch(()=>message.textContent='Could not load your order. Please return to the shop.');
